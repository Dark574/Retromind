<UserControl xmlns="https://github.com/avaloniaui"
             xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
             xmlns:vm="using:Retromind.ViewModels"
             xmlns:ext="clr-namespace:Retromind.Extensions;assembly=Retromind"
             xmlns:video="using:Retromind.Helpers.Video"
             xmlns:helpers="using:Retromind.Helpers"
             xmlns:resources="clr-namespace:Retromind.Resources;assembly=Retromind"
             x:DataType="vm:BigModeViewModel"
             x:Name="ThemeRoot"
             Background="#101010"

             ext:ThemeProperties.Name="Default System Layout"
             ext:ThemeProperties.Author="Retromind"
             ext:ThemeProperties.Version="0.1.0"
             ext:ThemeProperties.PrimaryVideoEnabled="True"
             ext:ThemeProperties.VideoSlotName="SystemPrimaryVideoSlot"
             ext:ThemeProperties.VideoFadeDurationMs="600"
             ext:ThemeProperties.SecondaryVideoEnabled="False">

    <!-- Very simple, neutral system layout:
         - large system title on the left
         - centered "monitor" with VideoSlot
         - basic stats at the bottom -->

    <Grid RowDefinitions="*,Auto"
          ColumnDefinitions="*,2.3*"
          Margin="40">

        <!-- LEFT: system title and short info -->
        <StackPanel Grid.Row="0"
                    Grid.Column="0"
                    VerticalAlignment="Center"
                    Spacing="8">
            <!-- Each subtheme instance keeps its own node while themes crossfade. -->
            <Image helpers:AsyncImageHelper.Url="{Binding ElementName=ThemeRoot, Path=(ext:ThemeProperties.SystemPreviewNode).PrimaryLogoAbsolutePath}"
                   Height="80"
                   Stretch="Uniform"
                   HorizontalAlignment="Center"
                   IsVisible="{Binding ElementName=ThemeRoot,
                                           Path=(ext:ThemeProperties.SystemPreviewNode).PrimaryLogoAbsolutePath,
                                                   Converter={x:Static helpers:StringConverters.IsNotNullOrEmpty}}"/>

            <!-- Fallback: text title if no logo -->
            <TextBlock Text="{Binding ElementName=ThemeRoot, Path=(ext:ThemeProperties.SystemPreviewNode).Name}"
                       FontSize="40"
                       FontWeight="Bold"
                       Foreground="White"
                       TextTrimming="CharacterEllipsis"
                       MaxWidth="260"
                       IsVisible="{Binding ElementName=ThemeRoot,
                                           Path=(ext:ThemeProperties.SystemPreviewNode).PrimaryLogoAbsolutePath,
                                                       Converter={x:Static helpers:StringConverters.IsNullOrEmpty}}" />
        </StackPanel>

        <!-- RIGHT: simple "monitor" reserved for the secondary video channel -->
        <Grid Grid.Row="0"
              Grid.Column="1"
              HorizontalAlignment="Center"
              VerticalAlignment="Center"
              MinWidth="720"
              MinHeight="360">
            <!-- Outer frame -->
            <Border Background="#181818"
                    CornerRadius="16"
                    Padding="24"
                    HorizontalAlignment="Stretch"
                    VerticalAlignment="Stretch">
                <!-- Split into: left = logo/name, right = screen -->
                <Grid ColumnDefinitions="Auto,*"
                      RowDefinitions="*">
                    <!-- RIGHT SIDE: monitor with secondary video slot -->
                    <Grid Grid.Column="1"
                          RowDefinitions="*,Auto">
                        <!-- "Screen" area with video + fallback text -->
                        <Border x:Name="SystemPrimaryVideoSlot"
                                Grid.Row="0"
                                Width="640"
                                Height="360"
                                Background="#181818"
                                CornerRadius="8"
                                Padding="0"
                                Margin="0,0,0,12"
                                HorizontalAlignment="Center"
                                VerticalAlignment="Center">
                            <Grid>
                                <TextBlock Text="{x:Static resources:ThemeStrings.NoSystemVideo}"
                                           HorizontalAlignment="Center"
                                           VerticalAlignment="Center"
                                           Foreground="#505050"
                                           FontSize="18" />
                            </Grid>
                        </Border>

                        <!-- Basic stats below the screen -->
                        <StackPanel Grid.Row="1"
                                    Orientation="Horizontal"
                                    HorizontalAlignment="Right"
                                    Spacing="12">
                            <StackPanel Orientation="Horizontal" Spacing="4">
                                <TextBlock Text="{x:Static resources:ThemeStrings.GameCountLabel}"
                                           Foreground="#AAAAAA"
                                           FontSize="14" />
                                <TextBlock Text="{Binding ElementName=ThemeRoot, Path=(ext:ThemeProperties.SystemPreviewNode).Items.Count}"
                                           Foreground="#FFFFFF"
                                           FontSize="14" />
                            </StackPanel>
                        </StackPanel>
                    </Grid>
                </Grid>
            </Border>
        </Grid>
        
    </Grid>
</UserControl>
